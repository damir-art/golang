# Логические операторы
Логические операторы объединяют несколько операторов сравнения.

    &&
    ||
    !

Пример:

    a := 1

    if (a > 0) && (a != 0) {
      fmt.Println("a > 0 && a != 0")
    }
